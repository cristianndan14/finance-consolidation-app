"""Modelos de borde: lo que entra y sale del proceso.

`llm.py` describe lo que devuelve el modelo; `api.py` (Fase 4) lo que devuelven
las rutas. Separados a proposito de los dataclasses del dominio: el borde valida
datos que vienen de afuera y no se puede confiar en ellos.
"""
